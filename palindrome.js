/**
 * Determine wither the submitted string is a palindrome.
 *
 * Examples:
 * racecar (pass)
 * mom (pass)
 * tacocat (pass)
 * fizzbuzz (fail)
 *
 * @param {String} value
 * @return {Boolean}
 */
var isPalindrome = function(value) {
    // TODO
};
